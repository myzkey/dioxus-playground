name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always

jobs:
  # Build CSS first, share with other jobs
  build-css:
    name: Build CSS
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install

      - name: Build CSS
        run: bun run css

      - name: Upload CSS
        uses: actions/upload-artifact@v4
        with:
          name: css
          path: apps/web/assets/main.css

  check:
    name: Check
    needs: build-css
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download CSS
        uses: actions/download-artifact@v4
        with:
          name: css
          path: apps/web/assets

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - name: Run cargo check
        run: cargo check --workspace

  fmt:
    name: Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt

      - name: Run cargo fmt
        run: cargo fmt --all -- --check

  clippy:
    name: Clippy
    needs: build-css
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download CSS
        uses: actions/download-artifact@v4
        with:
          name: css
          path: apps/web/assets

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - name: Run clippy
        run: cargo clippy --workspace -- -D warnings

  test:
    name: Test
    needs: build-css
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download CSS
        uses: actions/download-artifact@v4
        with:
          name: css
          path: apps/web/assets

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - name: Run tests
        run: cargo test --workspace

  build-web:
    name: Build Web
    needs: build-css
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download CSS
        uses: actions/download-artifact@v4
        with:
          name: css
          path: apps/web/assets

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - name: Install Dioxus CLI
        uses: taiki-e/install-action@v2
        with:
          tool: dioxus-cli

      - name: Build Web
        run: dx build --platform web --package web --release
